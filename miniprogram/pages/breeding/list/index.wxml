<view class="page">
  <view class="filter-bar">
    <view class="filter-item" bindtap="toggleFilter">
      <text class="filter-text">筛选</text>
      <text class="filter-icon">▼</text>
    </view>
    <view class="quick-filters">
      <view class="quick-filter-item" bindtap="resetFilter">全部</view>
    </view>
  </view>

  <view class="filter-drawer {{showFilter ? 'show' : ''}}">
    <view class="filter-content">
      <view class="filter-group">
        <view class="filter-label">配对状态</view>
        <picker mode="selector" range="{{pairingStatusList}}" range-key="label" value="0" bindchange="onPairingStatusChange">
          <view class="filter-value">{{pairingStatusLabel}}</view>
        </picker>
      </view>

      <view class="filter-group">
        <view class="filter-label">品种</view>
        <picker mode="selector" range="{{breedList}}" range-key="label" value="0" bindchange="onBreedChange">
          <view class="filter-value">{{breedLabel}}</view>
        </picker>
      </view>

      <view class="filter-group">
        <view class="filter-label">性别</view>
        <picker mode="selector" range="{{genderList}}" range-key="label" value="0" bindchange="onGenderChange">
          <view class="filter-value">{{genderLabel}}</view>
        </picker>
      </view>

      <view class="filter-group">
        <view class="filter-label">搜索</view>
        <input class="filter-input" placeholder="搜索圈号或品种" value="{{filters.search}}" bindinput="onSearchInput" />
      </view>

      <view class="filter-actions">
        <button class="filter-btn reset" bindtap="resetFilter">重置</button>
        <button class="filter-btn apply" bindtap="applyFilter">应用</button>
      </view>
    </view>
  </view>

  <view class="list">
    <view
      class="breeding-card status-{{item.isPaired ? 'paired' : (item.status === 'incubating' ? 'incubating' : 'breeding')}}"
      wx:for="{{list}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="goDetail"
    >
      <view class="status-tag status-{{item.isPaired ? 'paired' : (item.status === 'incubating' ? 'incubating' : 'breeding')}}">
        <text>{{item.isPaired ? '♥' : (item.status === 'incubating' ? '🐣' : '●')}}</text>
        <text>{{item.statusText}}</text>
      </view>
      <view class="card-left">
        <view class="breed">{{item.breed}}</view>
        <view class="meta">{{item.gender}} · {{item.ring_number || '无圈号'}}</view>
        <view class="mate-info" wx:if="{{item.mateInfo}}">
          <text class="mate-icon">♥</text>
          <text class="mate-text">{{item.mateInfo.breed}} ({{item.mateInfo.ring_number || '无圈号'}})</text>
          <text class="mate-duration">{{item.pairingDuration}}</text>
        </view>
      </view>
      <view class="card-right">
        <view class="action-buttons">
          <button
            wx:if="{{!item.isPaired && item.gender === '公'}}"
            class="action-btn pair"
            catchtap="goPair"
            data-id="{{item.id}}"
          >配对</button>
          <button
            wx:if="{{!item.isPaired && item.gender === '母'}}"
            class="action-btn pair"
            catchtap="goPair"
            data-id="{{item.id}}"
          >配对</button>
          <button
            wx:if="{{!item.isPaired && item.status === 'breeding'}}"
            class="action-btn remove"
            catchtap="removeFromBreeding"
            data-id="{{item.id}}"
          >移除</button>
          <button
            wx:if="{{item.isPaired && item.status !== 'incubating'}}"
            class="action-btn unpair"
            catchtap="unpairParrot"
            data-id="{{item.id}}"
          >取消配对</button>
          <button
            wx:if="{{item.isPaired && item.status !== 'incubating'}}"
            class="action-btn incubation"
            catchtap="showIncubationDialog"
            data-id="{{item.id}}"
          >开始孵化</button>
          <button
            wx:if="{{item.status === 'incubating'}}"
            class="action-btn view-incubation"
            catchtap="goIncubationList"
          >查看孵化</button>
        </view>
      </view>
    </view>

    <view class="empty-state" wx:if="{{!list.length && !loading}}">
      <text class="empty-text">暂无种鸟</text>
    </view>
  </view>

  <view class="incubation-dialog" wx:if="{{showIncubationDialog}}">
    <view class="dialog-mask" bindtap="hideIncubationDialog"></view>
    <view class="dialog-content">
      <view class="dialog-header">
        <text class="dialog-title">开始孵化</text>
      </view>
      <view class="dialog-body">
        <view class="form-item">
          <text class="form-label"><text class="required">*</text> 蛋数</text>
          <input type="number" placeholder="请输入蛋数" value="{{incubationEggsCount}}" bindinput="onIncubationEggsInput" />
        </view>
      </view>
      <view class="dialog-footer">
        <button class="dialog-btn cancel" bindtap="hideIncubationDialog">取消</button>
        <button class="dialog-btn confirm" bindtap="confirmIncubation">确定</button>
      </view>
    </view>
  </view>
</view>
